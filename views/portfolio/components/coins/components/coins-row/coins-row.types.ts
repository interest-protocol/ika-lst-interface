export interface CoinsRowProps {
  id: string;
  price: number;
  value: number;
  symbol: string;
  iconUrl: string;
  balance: string;
}

export interface CoinsRowMetadataProps {
  iconUrl: string;
  symbol: string;
}
